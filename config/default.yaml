capture:
  type: gopro_udp
  address: udp://0.0.0.0:8554

gpu:
  backend: opencl

pipeline:
  threads: auto
  queue_size: 4

filters:
  - type: identity
    enabled: true

output:
  type: v4l2
  device: /dev/video20
