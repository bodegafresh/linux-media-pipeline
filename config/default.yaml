capture:
  type: gopro_udp
  address: udp://0.0.0.0:8554

gpu:
  backend: opencl

pipeline:
  threads: auto
  queue_size: 4

filters:
  - type: identity
    enabled: true
  - type: grayscale
    enabled: false
  - type: negative
    enabled: false
  - type: sepia
    enabled: false

output:
  type: v4l2
  device: /dev/video20
